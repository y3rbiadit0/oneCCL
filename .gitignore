# Generated by CMake from include/oneapi/ccl/config.h.in and copied back into the
# source tree on every configure (see CMakeLists.txt). Upstream stopped tracking it;
# committing it pins a stale build stamp and a stale set of CCL_ENABLE_* defines.
include/oneapi/ccl/config.h
